{
  "name": "prompt-artisan",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1", // Убедитесь, что он здесь появился
    "sass": "^1.77.2",
    "vite": "^5.2.11"
  }
}